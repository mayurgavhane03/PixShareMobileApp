import Items from "./Items" // Ensure the correct casing for the file name
import Category from "./category" // Capitalize the imported name

export const schemaTypes = [Category, Items] // Ensure consistent casing in the export
